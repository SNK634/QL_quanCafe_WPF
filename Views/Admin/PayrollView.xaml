<UserControl x:Class="QL_quanCafe.Views.Admin.PayrollView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" 
             d:DesignHeight="650" d:DesignWidth="750" Background="#F9F6F0">

    <!-- PHẦN TÀI NGUYÊN (RESOURCES) ĐÃ ĐƯỢC CHUYỂN LÊN ĐÂY ĐỂ TRÁNH LỖI XDG-000 -->
    <UserControl.Resources>
        <Style x:Key="CenterAlign" TargetType="TextBlock">
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
        <Style x:Key="RightAlign" TargetType="TextBlock">
            <Setter Property="HorizontalAlignment" Value="Right"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,0,5,0"/>
        </Style>
    </UserControl.Resources>

    <!-- BẮT ĐẦU GIAO DIỆN CHÍNH -->
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- HEADER -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,20">
            <Button Content="← Quay lại" Click="BtnBack_Click" Background="Transparent" Foreground="#6F4E37" FontWeight="Bold" BorderThickness="0" Cursor="Hand" Margin="0,0,20,0" FontSize="16"/>
            <TextBlock Text="💰 BẢNG TÍNH LƯƠNG NHÂN VIÊN" FontSize="24" FontWeight="Bold" Foreground="#4A2E1B" VerticalAlignment="Center"/>
        </StackPanel>

        <!-- BỘ LỌC & NÚT CHỨC NĂNG -->
        <Border Grid.Row="1" Background="White" CornerRadius="8" Padding="15" Margin="0,0,0,15">
            <Border.Effect>
                <DropShadowEffect BlurRadius="5" Color="Gray" Opacity="0.1" ShadowDepth="1"/>
            </Border.Effect>
            <Grid>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="Kỳ tính lương:" VerticalAlignment="Center" Margin="0,0,10,0" FontWeight="SemiBold" Foreground="#4A2E1B"/>
                    <ComboBox x:Name="CmbMonth" Width="90" Height="30" Margin="0,0,10,0" VerticalContentAlignment="Center">
                        <ComboBoxItem Content="Tháng 1"/>
                        <ComboBoxItem Content="Tháng 2"/>
                        <ComboBoxItem Content="Tháng 3"/>
                        <ComboBoxItem Content="Tháng 4"/>
                        <ComboBoxItem Content="Tháng 5"/>
                        <ComboBoxItem Content="Tháng 6"/>
                        <ComboBoxItem Content="Tháng 7"/>
                        <ComboBoxItem Content="Tháng 8"/>
                        <ComboBoxItem Content="Tháng 9"/>
                        <ComboBoxItem Content="Tháng 10"/>
                        <ComboBoxItem Content="Tháng 11"/>
                        <ComboBoxItem Content="Tháng 12"/>
                    </ComboBox>
                    <TextBlock Text="/" VerticalAlignment="Center" Margin="0,0,10,0" FontSize="16"/>
                    <TextBox x:Name="TxtYear" Width="60" Height="30" Margin="0,0,20,0" VerticalContentAlignment="Center" HorizontalContentAlignment="Center"/>

                    <Button Content="TÍNH LƯƠNG" Click="BtnCalculate_Click" Width="100" Height="35" Background="#1976D2" Foreground="White" FontWeight="Bold" Cursor="Hand" Margin="0,0,10,0"/>

                    <!-- NÚT XEM CÔNG THỨC LƯƠNG -->
                    <Button Content="ℹ️ XEM CÔNG THỨC" Click="BtnViewFormula_Click" Width="140" Height="35" Background="#FF9800" Foreground="White" FontWeight="Bold" Cursor="Hand"/>
                </StackPanel>

                <TextBlock Text="Đơn vị: VNĐ" FontStyle="Italic" Foreground="Gray" HorizontalAlignment="Right" VerticalAlignment="Center"/>
            </Grid>
        </Border>

        <!-- BẢNG DỮ LIỆU LƯƠNG -->
        <DataGrid x:Name="DgPayroll" Grid.Row="2" AutoGenerateColumns="False" CanUserAddRows="False" 
                  Background="White" RowHeight="35" HeadersVisibility="Column" AlternatingRowBackground="#F5F5F5"
                  HorizontalGridLinesBrush="#E0E0E0" VerticalGridLinesBrush="#E0E0E0" BorderBrush="#D7CCC8">
            <DataGrid.ColumnHeaderStyle>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="#6F4E37"/>
                    <Setter Property="Foreground" Value="White"/>
                    <Setter Property="FontWeight" Value="Bold"/>
                    <Setter Property="Padding" Value="10,5"/>
                    <Setter Property="HorizontalContentAlignment" Value="Center"/>
                </Style>
            </DataGrid.ColumnHeaderStyle>

            <DataGrid.Columns>
                <!-- Các cột liên kết dữ liệu với class PayrollRecord -->
                <DataGridTextColumn Header="Mã NV" Binding="{Binding Id}" Width="60" IsReadOnly="True"/>
                <DataGridTextColumn Header="Họ và Tên" Binding="{Binding FullName}" Width="*" IsReadOnly="True">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="FontWeight" Value="Bold"/>
                            <Setter Property="VerticalAlignment" Value="Center"/>
                            <Setter Property="Margin" Value="5,0"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="Chức Vụ" Binding="{Binding Role}" Width="90" IsReadOnly="True"/>

                <DataGridTextColumn Header="Số Ca Thường" Binding="{Binding SoCaThuong}" Width="100" IsReadOnly="True" ElementStyle="{StaticResource CenterAlign}"/>
                <DataGridTextColumn Header="Số Ca Lễ" Binding="{Binding SoCaLe}" Width="80" IsReadOnly="True" ElementStyle="{StaticResource CenterAlign}"/>
                <DataGridTextColumn Header="Sao (KPI)" Binding="{Binding SaoDanhGia}" Width="80" IsReadOnly="True" ElementStyle="{StaticResource CenterAlign}"/>

                <!-- Cột hiển thị tiền Format tự động có dấu phẩy (,000) -->
                <DataGridTextColumn Header="Lương Cơ Bản" Binding="{Binding LuongCoBan, StringFormat={}{0:N0}}" Width="110" IsReadOnly="True" ElementStyle="{StaticResource RightAlign}"/>
                <DataGridTextColumn Header="Lương Lễ" Binding="{Binding LuongLe, StringFormat={}{0:N0}}" Width="90" IsReadOnly="True" ElementStyle="{StaticResource RightAlign}"/>

                <DataGridTextColumn Header="Thưởng/Phạt" Binding="{Binding ThuongPhat, StringFormat={}{0:N0}}" Width="100" IsReadOnly="True">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="Foreground" Value="#E64A19"/>
                            <Setter Property="HorizontalAlignment" Value="Right"/>
                            <Setter Property="VerticalAlignment" Value="Center"/>
                            <Setter Property="Margin" Value="0,0,5,0"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>

                <DataGridTextColumn Header="TỔNG THỰC LÃNH" Binding="{Binding TotalSalary, StringFormat={}{0:N0}}" Width="130" IsReadOnly="True">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="Foreground" Value="#2E7D32"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                            <Setter Property="FontSize" Value="14"/>
                            <Setter Property="HorizontalAlignment" Value="Right"/>
                            <Setter Property="VerticalAlignment" Value="Center"/>
                            <Setter Property="Margin" Value="0,0,5,0"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>

        <!-- NÚT XUẤT FILE Ở DƯỚI CÙNG -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="📊 Xuất Excel" Click="BtnExportExcel_Click" Width="110" Height="40" Background="#388E3C" Foreground="White" FontWeight="Bold" Cursor="Hand" Margin="0,0,15,0"/>
            <Button Content="🖨 In Phiếu Lương" Click="BtnPrint_Click" Width="130" Height="40" Background="#5D4037" Foreground="White" FontWeight="Bold" Cursor="Hand"/>
        </StackPanel>

    </Grid>
</UserControl>