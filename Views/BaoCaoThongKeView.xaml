<UserControl x:Class="QL_quanCafe.BaoCaoThongKeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="950">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 1. BỘ LỌC THỜI GIAN -->
        <Border Grid.Row="0" Background="#EDF6F3" CornerRadius="8" Padding="12" Margin="0,0,0,15">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="Từ ngày:" VerticalAlignment="Center" FontWeight="Bold" Margin="0,0,8,0"/>
                <DatePicker x:Name="dpTuNgay" Width="140" Margin="0,0,15,0"/>

                <TextBlock Text="Đến ngày:" VerticalAlignment="Center" FontWeight="Bold" Margin="0,0,8,0"/>
                <DatePicker x:Name="dpDenNgay" Width="140" Margin="0,0,15,0"/>

                <Button Content="📊 Thống kê" Click="BtnThongKe_Click" Width="110" Height="32" 
                        Background="#319795" Foreground="White" FontWeight="Bold" 
                        BorderThickness="0" Cursor="Hand"/>
            </StackPanel>
        </Border>

        <!-- 2. CÁC THẺ THỐNG KÊ NHANH -->
        <UniformGrid Grid.Row="1" Columns="3" Margin="0,0,0,15">
            <!-- Thẻ Doanh thu -->
            <Border Background="#4299E1" CornerRadius="8" Padding="15" Margin="0,0,10,0">
                <StackPanel>
                    <TextBlock Text="TỔNG DOANH THU" Foreground="White" FontSize="12" FontWeight="Bold"/>
                    <TextBlock x:Name="txtTongDoanhThu" Text="0 đ" Foreground="White" FontSize="20" FontWeight="Bold" Margin="0,5,0,0"/>
                </StackPanel>
            </Border>

            <!-- Thẻ Đơn hàng -->
            <Border Background="#48BB78" CornerRadius="8" Padding="15" Margin="5,0,5,0">
                <StackPanel>
                    <TextBlock Text="TỔNG SỐ ĐƠN HÀNG" Foreground="White" FontSize="12" FontWeight="Bold"/>
                    <TextBlock x:Name="txtTongDonHang" Text="0" Foreground="White" FontSize="20" FontWeight="Bold" Margin="0,5,0,0"/>
                </StackPanel>
            </Border>

            <!-- Thẻ Lợi nhuận / Số món bán -->
            <Border Background="#ED8936" CornerRadius="8" Padding="15" Margin="10,0,0,0">
                <StackPanel>
                    <TextBlock Text="TRUNG BÌNH / ĐƠN" Foreground="White" FontSize="12" FontWeight="Bold"/>
                    <TextBlock x:Name="txtTrungBinhDon" Text="0 đ" Foreground="White" FontSize="20" FontWeight="Bold" Margin="0,5,0,0"/>
                </StackPanel>
            </Border>
        </UniformGrid>

        <!-- 3. BẢNG DANH SÁCH HÓA ĐƠN -->
        <DataGrid Grid.Row="2" x:Name="dgHoaDon" AutoGenerateColumns="False" IsReadOnly="True"
                  GridLinesVisibility="Horizontal" RowHeight="35" Background="White"
                  BorderBrush="#CBD5E0" BorderThickness="1" ColumnWidth="*">
            <DataGrid.Resources>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="#4A2E1B"/>
                    <Setter Property="Foreground" Value="White"/>
                    <Setter Property="FontWeight" Value="Bold"/>
                    <Setter Property="Height" Value="35"/>
                    <Setter Property="HorizontalContentAlignment" Value="Center"/>
                </Style>
            </DataGrid.Resources>
            <DataGrid.Columns>
                <DataGridTextColumn Header="Mã HĐ" Binding="{Binding Id}" Width="80"/>
                <DataGridTextColumn Header="Ngày lập" Binding="{Binding NgayTao, StringFormat={}{0:dd/MM/yyyy HH:mm}}" Width="180"/>
                <DataGridTextColumn Header="Thu ngân" Binding="{Binding TenThuNgan}" Width="150"/>
                <DataGridTextColumn Header="Tổng tiền" Binding="{Binding TongTien, StringFormat={}{0:N0}đ}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>