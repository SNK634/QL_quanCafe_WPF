<UserControl x:Class="QL_quanCafe.BanHangView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="950">
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <!-- Cột 1: Danh mục bên trái -->
            <ColumnDefinition Width="160"/>
            <!-- Cột 2: Danh sách món ăn ở giữa -->
            <ColumnDefinition Width="*"/>
            <!-- Cột 3: Giỏ hàng & Tổng tiền bên phải (MỚI THÊM) -->
            <ColumnDefinition Width="280"/>
        </Grid.ColumnDefinitions>

        <!-- DANH MỤC THỰC ĐƠN -->
        <Border Grid.Column="0" Background="#EDF6F3" CornerRadius="8" Padding="10" Margin="0,0,10,0">
            <StackPanel>
                <TextBlock Text="DANH MỤC THỰC ĐƠN" FontWeight="Bold" FontSize="12" Foreground="#2D3748" Margin="0,5,0,15"/>
                <ListBox x:Name="lstCategory"
         Background="Transparent"
         BorderThickness="0"
         FontSize="13"
         SelectionChanged="LstCategory_SelectionChanged">

                    <ListBoxItem Content="Tất cả" IsSelected="True" Margin="0,3" Tag="All"/>

                    <ListBoxItem Content="☕ Café" Margin="0,3" Tag="Cafe"/>

                    <ListBoxItem Content="🍵 Trà" Margin="0,3" Tag="Tra"/>

                    <ListBoxItem Content="🥤 Đá Xay" Margin="0,3" Tag="DaXay"/>

                    <ListBoxItem Content="🍊 Nước Ép" Margin="0,3" Tag="NuocEp"/>

                    <ListBoxItem Content="🧋 Trà Sữa" Margin="0,3" Tag="TraSua"/>

                    <ListBoxItem Content="🥣 Yogurt" Margin="0,3" Tag="Yogurt"/>

                    <ListBoxItem Content="➕ Topping" Margin="0,3" Tag="Topping"/>

                </ListBox>
            </StackPanel>
        </Border>

        <!-- DANH SÁCH MÓN ăn (ITEMSCHECK/CARD) -->
        <ScrollViewer Grid.Column="1" VerticalScrollBarVisibility="Auto" Margin="0,0,10,0">
            <ItemsControl x:Name="icProducts">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="#E2E8F0" BorderThickness="0,0,0,1" Padding="10" Margin="0,0,0,5">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="{Binding Name}" FontWeight="Bold" FontSize="14" Foreground="#2D3748"/>
                                    <TextBlock Text="{Binding SubName}" FontSize="11" Foreground="#A0AEC0" Margin="0,2,0,5"/>
                                    <TextBlock Text="{Binding Price, StringFormat={}{0:N0}đ}" FontWeight="Bold" FontSize="13" Foreground="#2B6CB0"/>
                                </StackPanel>

                                <!-- NÚT "CHỌN" ĐÃ ĐƯỢC ĐỔI TỪ "XEM NHANH" -->
                                <Button Grid.Column="1" Content="Chọn" Click="BtnSelectProduct_Click" 
                                        Tag="{Binding}" VerticalAlignment="Center" Padding="15,6" 
                                        Background="#EDF2F7" Foreground="#2D3748" FontWeight="SemiBold" 
                                        BorderBrush="#CBD5E0" BorderThickness="1" Cursor="Hand"/>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <!-- CỘT GIỎ HÀNG & TỔNG TIỀN (MỚI THÊM) -->
        <Border Grid.Column="2" Background="#FFFFFF" BorderBrush="#CBD5E0" BorderThickness="1" CornerRadius="8" Padding="12">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="MÓN ĐÃ CHỌN" FontWeight="Bold" FontSize="14" Foreground="#4A2E1B" Margin="0,0,0,10" HorizontalAlignment="Center"/>

                <!-- BẢNG DANH SÁCH MÓN ĐÃ CHỌN -->
                <DataGrid Grid.Row="1" x:Name="dgCart" AutoGenerateColumns="False" IsReadOnly="True" 
                          GridLinesVisibility="Horizontal" BorderThickness="0" Background="Transparent">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Tên món" Binding="{Binding Name}" Width="*"/>
                        <DataGridTextColumn Header="SL" Binding="{Binding Quantity}" Width="35"/>
                        <DataGridTextColumn Header="Thành tiền" Binding="{Binding TotalPrice, StringFormat={}{0:N0}đ}" Width="80"/>
                    </DataGrid.Columns>
                </DataGrid>

                <!-- KHU VỰC TỔNG TIỀN & THANH TOÁN -->
                <StackPanel Grid.Row="2" Margin="0,10,0,0">
                    <Separator Margin="0,0,0,10"/>
                    <DockPanel Margin="0,0,0,10">
                        <TextBlock Text="TỔNG TIỀN:" FontWeight="Bold" FontSize="14" Foreground="#2D3748"/>
                        <TextBlock x:Name="txtGrandTotal" Text="0 đ" FontWeight="Bold" FontSize="16" Foreground="#E53E3E" HorizontalAlignment="Right"/>
                    </DockPanel>
                    <Button Content="THANH TOÁN" Height="40" Background="#319795" Foreground="White" 
        FontWeight="Bold" FontSize="14" BorderThickness="0" Cursor="Hand"
        Click="BtnCheckout_Click"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>